# Groundwork CLI Source

This `src/` tree owns the root Groundwork CLI protocol, CLI metadata, and tests. Shared foundation behavior lives in `../packages/core/src/`. Multi-harness hooks and tools live in `../prism-plugin/` and are compiled with Prism.
